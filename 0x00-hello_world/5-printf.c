#include <stdio.h>
/**
 * main - This is a programme that prints using the printf function
 * Return: (0) (Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
